import React from 'react';

/** Страница «Таблицы» — заглушка для дальнейшего наполнения. */
function PageTables() {
  return (
    <section className="card tracker-card page-placeholder">
      <h2 className="page-placeholder-title">Таблицы</h2>
      <p className="page-placeholder-text">
        Здесь будут таблицы с данными. Раздел в разработке.
      </p>
    </section>
  );
}

export default PageTables;
